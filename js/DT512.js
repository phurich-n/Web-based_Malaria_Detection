if (hist[202] <= 0) {
    if (hist[206] <= 0) {
        if (hist[214] <= 0) {
            if (hist[232] <= 0) {
                if (hist[234] <= 0) {
                    if (hist[210] <= 0) {
                        if (hist[190] <= 0) {
                            classes = 'Uninfected';
                        } else {
                            classes = 'Uninfected';
                        }
                    } else {
                        if (hist[124] <= 0.079) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Uninfected';
                        }
                    }
                } else {
                    if (hist[86] <= 0) {
                        if (hist[256] <= 0) {
                            classes = 'Uninfected';
                        } else {
                            classes = 'Parasitized';
                        }
                    } else {
                        classes = 'Parasitized';
                    }
                }
            } else {
                if (hist[246] <= 0.001) {
                    if (hist[88] <= 0.231) {
                        if (hist[402] <= 0.001) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Uninfected';
                        }
                    } else {
                        classes = 'Uninfected';
                    }
                } else {
                    if (hist[114] <= 0.193) {
                        classes = 'Uninfected';
                    } else {
                        classes = 'Parasitized';
                    }
                }
            }
        } else {
            if (hist[394] <= 0.002) {
                if (hist[172] <= 0.138) {
                    if (hist[140] <= 0.238) {
                        if (hist[122] <= 0.317) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Uninfected';
                        }
                    } else {
                        if (hist[278] <= 0) {
                            classes = 'Uninfected';
                        } else {
                            classes = 'Parasitized';
                        }
                    }
                } else {
                    classes = 'Uninfected';
                }
            } else {
                classes = 'Uninfected';
            }
        }
    } else {
        if (hist[162] <= 0.026) {
            if (hist[378] <= 0.002) {
                if (hist[156] <= 0.214) {
                    if (hist[262] <= 0) {
                        if (hist[300] <= 0) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Parasitized';
                        }
                    } else {
                        if (hist[34] <= 0.348) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Uninfected';
                        }
                    }
                } else {
                    classes = 'Uninfected';
                }
            } else {
                classes = 'Uninfected';
            }
        } else {
            if (hist[242] <= 0) {
                if (hist[240] <= 0) {
                    if (hist[370] <= 0) {
                        if (hist[172] <= 0.263) {
                            classes = 'Uninfected';
                        } else {
                            classes = 'Parasitized';
                        }
                    } else {
                        classes = 'Parasitized';
                    }
                } else {
                    classes = 'Parasitized';
                }
            } else {
                if (hist[376] <= 0.001) {
                    if (hist[188] <= 0.007) {
                        classes = 'Parasitized';
                    } else {
                        classes = 'Uninfected';
                    }
                } else {
                    classes = 'Uninfected';
                }
            }
        }
    }
} else {
    if (hist[288] <= 0) {
        if (hist[202] <= 0.016) {
            if (hist[238] <= 0) {
                if (hist[162] <= 0.019) {
                    if (hist[146] <= 0.026) {
                        if (hist[192] <= 0.005) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Uninfected';
                        }
                    } else {
                        if (hist[252] <= 0) {
                            classes = 'Uninfected';
                        } else {
                            classes = 'Parasitized';
                        }
                    }
                } else {
                    if (hist[262] <= 0) {
                        if (hist[246] <= 0) {
                            classes = 'Uninfected';
                        } else {
                            classes = 'Parasitized';
                        }
                    } else {
                        classes = 'Parasitized';
                    }
                }
            } else {
                if (hist[238] <= 0.006) {
                    if (hist[282] <= 0) {
                        if (hist[294] <= 0) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Parasitized';
                        }
                    } else {
                        if (hist[32] <= 0.436) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Uninfected';
                        }
                    }
                } else {
                    if (hist[156] <= 0.006) {
                        classes = 'Parasitized';
                    } else {
                        if (hist[210] <= 0.006) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Uninfected';
                        }
                    }
                }
            }
        } else {
            if (hist[294] <= 0) {
                if (hist[368] <= 0) {
                    if (hist[318] <= 0) {
                        if (hist[196] <= 0.091) {
                            classes = 'Uninfected';
                        } else {
                            classes = 'Uninfected';
                        }
                    } else {
                        if (hist[360] <= 0) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Uninfected';
                        }
                    }
                } else {
                    classes = 'Parasitized';
                }
            } else {
                if (hist[274] <= 0.011) {
                    if (hist[0] <= 0.928) {
                        classes = 'Parasitized';
                    } else {
                        if (hist[360] <= 0) {
                            classes = 'Uninfected';
                        } else {
                            classes = 'Parasitized';
                        }
                    }
                } else {
                    classes = 'Uninfected';
                }
            }
        }
    } else {
        if (hist[246] <= 0.017) {
            if (hist[48] <= 0.115) {
                if (hist[120] <= 0.626) {
                    if (hist[294] <= 0.149) {
                        if (hist[32] <= 0.514) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Parasitized';
                        }
                    } else {
                        classes = 'Uninfected';
                    }
                } else {
                    classes = 'Uninfected';
                }
            } else {
                if (hist[352] <= 0.002) {
                    if (hist[60] <= 0.011) {
                        if (hist[98] <= 0.002) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Uninfected';
                        }
                    } else {
                        if (hist[84] <= 0) {
                            classes = 'Uninfected';
                        } else {
                            classes = 'Parasitized';
                        }
                    }
                } else {
                    classes = 'Uninfected';
                }
            }
        } else {
            if (hist[358] <= 0) {
                if (hist[348] <= 0) {
                    if (hist[472] <= 0) {
                        if (hist[106] <= 0.002) {
                            classes = 'Uninfected';
                        } else {
                            classes = 'Parasitized';
                        }
                    } else {
                        classes = 'Parasitized';
                    }
                } else {
                    if (hist[142] <= 0.029) {
                        classes = 'Parasitized';
                    } else {
                        if (hist[444] <= 0) {
                            classes = 'Uninfected';
                        } else {
                            classes = 'Parasitized';
                        }
                    }
                }
            } else {
                if (hist[276] <= 0) {
                    classes = 'Uninfected';
                } else {
                    if (hist[182] <= 0.002) {
                        classes = 'Uninfected';
                    } else {
                        if (hist[186] <= 0.036) {
                            classes = 'Parasitized';
                        } else {
                            classes = 'Parasitized';
                        }
                    }
                }
            }
        }
    }
}